# Format sts2 ids to a prettier form.

Format sts2 ids to a prettier form.

## Usage

``` r
format_sts2id(id)
```

## Arguments

- id:

  The id/s to format.

## Value

The formatted ids.

## Note

This assumes that ids have only 1 "." character.
